attach(Bank)
grap_s = barplot(table(Bank$sex), col=c("blue", "red"))

grap_ = barplot(table(Bank$sex), col=c("blue", "red"))

grap_sexrace = barplot(table(Bank$sexrace), col=c("green", "orange", "gray"))

graph_jobcat = barplot(table(Bank$jobcat), col=c("purple", "yellow", "lightblue"))

