var minhaVar = 'Teste'

function soma(x, y) {
  return x + y
}

let numero = 10

const PI = 3.14

let numeros = [1, 2, 3]

numeros.map(i => i + 5)


class Matematica {
  soma(x, y) {
    return x + y
  }
}