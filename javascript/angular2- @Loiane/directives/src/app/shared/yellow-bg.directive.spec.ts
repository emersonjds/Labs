import { YellowBgDirective } from './yellow-bg.directive';

describe('YellowBgDirective', () => {
  it('should create an instance', () => {
    const directive = new YellowBgDirective();
    expect(directive).toBeTruthy();
  });
});
