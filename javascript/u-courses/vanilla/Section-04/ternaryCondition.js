condition ? true : false;

1 === 2 ? true : false; // return false

var pessoa = {
    sexo: 'Masculino'
}

var sexo = pessoa.sexo === 'Feminino' ? 'a' : 'o';

1 ? true : false; // return true, because 1 it's truthy 