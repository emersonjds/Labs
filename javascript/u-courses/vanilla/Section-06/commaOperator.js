var a, b = 2, c;

function myFunction() {
    var x = 0;
    return (x += 1, x);
}

let b = 1, c = 4; 
// With reserved word let or var, the variables received the values in sequence, but the ideal is use always var or let in each variable declareted