window.onload = function () {
    console.log('iudhusg');

    2 + 2; // int data types
    // return 4

    2 + '22'; // Javascript convert a first number in string and sum both in a concatenation 
    // return 22

    'Hello' + ' World'; // return 'Hello World'

    10 > 2 // return true
    10 < 2 // return false

    // when a variable isn't value , the javscript return a undefined value in call of this variable,
    // undefined value seems witch variable don't have value statement
    let casa; // return null because a value is not passed;

    //null value 
    let casa = null; // the value is passed witch clear;

    typeof (300 / 2); // number 
    typeof ('Hello World'); // string 
    typeof (4 < 2); // false 
    typeof (a + b); // is not defined variables
    typeof (3 + '6'); // string 
}