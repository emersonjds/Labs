#include <stdio.h>
#include <stdlib.h>

int main()
{
    int idade = 0;
    printf("Informe sua idade: \n");
    scanf("%d", &”dade);
    if (idade >= 18 ) {
        printf("Voce e maior de idade , voce tem %d anos de idade", idade );
    } else {
         printf("Voce nao e de idade , voce tem %d anos de idade", idade );
    }
    return 0;
}
