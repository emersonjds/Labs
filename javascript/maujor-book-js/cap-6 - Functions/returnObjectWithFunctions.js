
calcula((b, h) => {
    area = b * h;
    perimetro = (b * h) * 2

    return {
        area: area,
        perimetro: perimetro
    }
})

function calcula(b, h) {
    area = b * h;
    perimetro = (b * h) * 2

    return {
        area: area,
        perimetro: perimetro
    }
}

