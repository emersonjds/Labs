//Converte a string passada em um numero inteiro, admite um argumento opcional , que é a base na qual o numero devera ser retornado.Caso o argumento nao seja passado a conversão é feita na base 10.

parseInt(int[,base])

function numeroParseado(x) {
    eval(`var num = parseInt(x); console.log(num)`)
}