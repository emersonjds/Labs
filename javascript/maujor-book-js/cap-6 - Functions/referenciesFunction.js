function calculaArea(a, b) {
    return (a + b)
}

var calculo = calculaArea; //referencie for variable 

calculo(10, 20)

    (function calcular() {
        area = 10;
        perimetro = 50
        return area * perimetro
    })()