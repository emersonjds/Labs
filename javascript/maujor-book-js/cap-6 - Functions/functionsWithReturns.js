//Função estática
function calculaArea(a, h) {
    return a * h
}

//es6Next
calculaPerimetro = (a, h) => a * h;
//chamada da funcao calculaPerimetro(param1, param 2)

//Este é um exemplo de funcao pura
//chamada da funcao calculo(param1, param 2)
calculo = function(a,b) {
    return a + b
}
