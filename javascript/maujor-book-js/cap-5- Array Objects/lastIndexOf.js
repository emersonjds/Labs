//retorna o ultimo indice do array, tem a mesma estrutura lexica que o indexOf e caso nao encontre o valor retorna com -1

//Ex:

var array = ["a", 3, 4, 5, "a", "3", 45]
array.indexOf("a") // retorna 0
array.lastIndexOf("a") // retorna 4 pois foi a ultima chamada encontrada para o argumento
