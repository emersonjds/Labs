//O metodo concat() concatena duas ou mais strings efinidas nos parametros stringsN

// A string resultante é fornada pela junção das strings ncatenadas. Esse método é não destrutivo, ou seja, as strings a serem reunidas são preservadas e uma nova string é criada.

var string1 = "Casa";
var string2 = " é um bom lgar para";

var stringResultante = string1.concat(string)
console.log(stringResultante);