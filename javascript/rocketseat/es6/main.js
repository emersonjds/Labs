import { soma } from './functions';
// import mult, {soma, sub} from './functions';
import mult, * as functions from './functions';

console.log(soma(1,3))
console.log(functions.sub(4, 1))
console.log(mult(2, 3));