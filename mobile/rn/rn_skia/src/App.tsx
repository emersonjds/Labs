import React from 'react';
import {SafeAreaView, Text, View} from 'react-native';

const App: React.FC = () => (
  <SafeAreaView>
    <View
      style={{
        padding: 10,
      }}>
      <Text>Skia Studies</Text>
    </View>
  </SafeAreaView>
);

export default App;
